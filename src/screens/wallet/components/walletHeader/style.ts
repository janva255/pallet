import { StyleSheet } from "react-native";

export default StyleSheet.create({
  headerContainer: {
    flexDirection: "row",
    paddingHorizontal: 20,
    alignItems: "center",
  },
  walletImage: { width: 38, height: 40 },
});
