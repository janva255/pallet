import { Text, View } from "react-native";
import styles from "styles";

export default () => (
  <View style={{ paddingHorizontal: 20 }}>
    <Text style={styles.text.logoText}>PALLET</Text>
  </View>
);
